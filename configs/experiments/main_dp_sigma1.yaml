protocol: configs/protocols/hai_shared_context.yaml
model: configs/models/fedhgf.yaml

privacy:
  enabled: true
  secure_aggregation_backend: assumed
  delta: 1.0e-5
  server_visible_channels:
    graph:
      clip_norm: 1.0
      noise_multiplier: 1.0
    encoder:
      clip_norm: 1.0
      noise_multiplier: 1.0
    center:
      clip_norm: 1.0
      noise_multiplier: 1.0
  local_channels:
    auxiliary_graph:
      dp_noise: false
    local_flow:
      dp_noise: false
    calibration:
      dp_noise: false
